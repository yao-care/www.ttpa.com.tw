---
// 全站共用頁尾，文字主體逐字取自 .source/01-home.md:108-129（頁尾 Footer 區塊）。
// 原站「快速連結」三項中，「加入會員」「最新消息」href 原為 `#`（見 01-home.md:125-126，
// 草稿未接完的連結）；「組織章程」href 原站已是一個真實外部網址（01-home.md:124，
// 指向舊 Google Sites 之章程子頁），非 `#`。
// 新站三項皆已接上對應真實頁面（組織章程/加入會員/最新消息），
// 這是接通既有標籤的既定路由，非新增文案。
// 2026-07-28 獨立驗收（.source/_final-audit.md P4）指出前一版註解誤稱三項原始 href 皆為
// `#`，已依 01-home.md 原文修正此註解（不影響實際頁面行為，三項本就正確導向真實內部頁面）。
// 2026-08-11 依資訊層級檢視，完整聯絡資料集中於 /contact/ 單一維護；頁尾只保留
// 服務入口，避免會員頁、聯絡頁與各活動頁在相鄰區塊重複電話、信箱及地址。
const base = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
---

<footer class="site-footer">
  <div class="container footer-inner">
    <div class="footer-brand">
      <img src={`${base}logo.png`} alt="臺灣遠距藥事照護協會 TTPA" width="56" height="33" />
      <p class="footer-desc">
        臺灣遠距藥事照護協會致力於提升醫療專業價值，透過科技賦能，實現無圍牆的醫療照護服務。
      </p>
    </div>

    <div class="footer-col footer-service">
      <h2>服務入口</h2>
      <p>入會、活動或合作相關問題，請由聯絡頁取得最新窗口資訊。</p>
      <a class="footer-contact" href={`${base}contact/`}>前往聯絡我們 <span aria-hidden="true">→</span></a>
    </div>

    <div class="footer-col">
      <h2>快速連結</h2>
      <ul class="plain">
        <li><a href={`${base}charter/`}>組織章程</a></li>
        <li><a href={`${base}membership/`}>加入會員</a></li>
        <li><a href={`${base}news/`}>最新消息</a></li>
        <li><a href={`${base}results/`}>活動成果</a></li>
      </ul>
    </div>
  </div>

  <p class="copyright">© 2026 臺灣遠距藥事照護協會 (Taiwan Telepharmacy Association). All rights reserved.</p>
</footer>

<style>
  .site-footer {
    position: relative;
    overflow: hidden;
    background: var(--color-primary-dark);
    margin-top: clamp(1rem, 3vw, 2rem);
    padding-block: clamp(1.25rem, 3vw, 1.75rem);
    color: var(--color-white);
  }

  .site-footer::before {
    position: absolute;
    width: 12rem;
    height: 12rem;
    border-radius: 50%;
    background: var(--color-white-a10);
    content: '';
    inset: -7rem -5rem auto auto;
    pointer-events: none;
  }

  .footer-inner {
    display: grid;
    gap: 1.25rem;
    grid-template-columns: 1fr;
  }

  .footer-brand img {
    width: 3.25rem;
    height: auto;
    margin-bottom: 0.5rem;
    padding: 0.4rem;
    border-radius: 0.7rem;
    background: var(--color-white);
  }

  .footer-desc {
    color: var(--color-brand-tint);
    font-size: var(--text-xs);
    line-height: 1.55;
    max-width: 28rem;
    margin: 0;
  }

  .footer-col h2 {
    font-size: var(--text-xs);
    color: var(--color-white);
    margin-block: 0 0.4rem;
  }

  .footer-service p {
    max-width: 19rem;
    margin: 0 0 0.35rem;
    color: var(--color-brand-tint);
    font-size: var(--text-xs);
    line-height: 1.5;
  }

  .footer-contact {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    color: var(--color-white);
    font-size: var(--text-xs);
    font-weight: 700;
    text-underline-offset: 0.2rem;
  }

  .footer-contact:hover {
    color: var(--color-brand-tint);
  }

  ul.plain {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    font-size: var(--text-xs);
    line-height: 1.5;
    color: var(--color-brand-tint);
  }

  ul.plain a {
    color: var(--color-white);
  }

  ul.plain a:hover {
    color: var(--color-brand-tint);
  }

  .copyright {
    margin: 1rem auto 0;
    max-width: 72rem;
    padding-inline: clamp(1rem, 4vw, 2rem);
    padding-top: 0.75rem;
    border-top: 1px solid var(--color-white-a20);
    font-size: var(--text-xs);
    color: var(--color-brand-tint);
  }

  @media (min-width: 768px) {
    .footer-inner {
      grid-template-columns: 2.2fr 1.3fr 1fr;
    }

    .footer-brand {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 0.8rem;
      align-items: start;
    }

    .footer-brand img {
      margin-bottom: 0;
    }
  }
</style>
